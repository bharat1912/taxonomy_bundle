# Conda environment file based on pixi.toml contents
channels:
  - defaults
  - nanoporetech
  - bioconda
  - conda-forge
  - defaults

dependencies:
# Using the specific version from your pixi.toml
  - medaka==1.8.0

# Adding required dependencies for Medaka
  - minimap2
  - samtools
